spring:
  profiles:
    active: native
  cloud:
    config:
      server:
       native:
         #search-locations: "classpath:/config"
         search-locations: "file:///D://Tech//projects//config"
  application:
    name: configserver
  

  
  
server:
  port: 8071