server:
  port: 8761
eureka:
  instance:
    hostname: "localhost"
  client:
    fetchRegistry: false
    registreWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
